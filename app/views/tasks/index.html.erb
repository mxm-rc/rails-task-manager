<h1>Tasks</h1>

<% @tasks.each do |task| %>
  <li> <%= link_to task.title, task_path(task) %> </li>
<% end %>

<%= form_with model: @task do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %>
  <%= f.label :details %>
  <%= f.text_field :details %>
  <%= f.submit%>
<% end %>
